export default {};

// custom configurations
const config = {
  showSidebar: true, // Add a table of content sidebar
  search: {          // Add a search bar
    provider: "kbar", 
  },
  // navLinks: [{ href: "/about", name: "About" }], // Add a navigation bar soon

};
